"""
API router package.

Endpoint groups live under this router and are included by the main
application. Currently empty; routes will be added as features are built.
"""
from fastapi import APIRouter

api_router = APIRouter()
